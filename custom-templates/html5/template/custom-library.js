// this is a sample custom .js library

var CustomLibrary = CustomLibrary || {};

/**
 * Adds two numbers.
 * 
 * @param {number} a 
 * @param {number} b 
 * @returns the sum of two numbers
 */
CustomLibrary.add = function(a, b) {
	return a + b;
}